// components

// style
import * as LC from './style';

// types
import { StyledSystemDefaultProps } from '~/types/Common';

interface P extends StyledSystemDefaultProps {}

const Name: React.FC<P> = ({ mt, mr, mb, ml, pt, pr, pb, pl }) => {
  return <></>;
};
export default Name;
